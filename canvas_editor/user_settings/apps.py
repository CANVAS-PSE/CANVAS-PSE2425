from django.apps import AppConfig


class UserSettingsConfig(AppConfig):
    """Config for the user settings django app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "user_settings"
