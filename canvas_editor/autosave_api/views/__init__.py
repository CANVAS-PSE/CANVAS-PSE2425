"""Contains all the api views.

A list view is used to list all available objects of that type and to create new ones. Sometimes some attributes of the listed model are not shown,
for that the detail view should be used.

A detail view is used to view the model in detail, update or delete it.

So for every object where multiple instances exist, a list view and a detail view are needed
"""
