from django.apps import AppConfig


class ProjectManagementConfig(AppConfig):
    """Configuration for the project_management app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "project_management"
