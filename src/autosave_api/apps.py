from django.apps import AppConfig


class AutosaveApiConfig(AppConfig):
    """Configuration for the autosave_api app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "autosave_api"
