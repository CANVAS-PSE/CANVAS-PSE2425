from django.apps import AppConfig


class JobInterfaceConfig(AppConfig):
    """Configuration for the job_interface app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "job_interface"
